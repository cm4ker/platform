namespace CodeGeneration.Configuration
{
    /// <summary>
    /// Represents raw data table with optimizations
    /// For example. I need store any information of object but I can't store it into entity, because the cost of extraction this data is a giant
    /// In this case you need to use RapidEntity. 
    /// </summary>
    public class RapidEntityConfigurationNode : ConfigurationNode
    {

    }
}