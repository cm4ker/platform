<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:ZenPlatform.SimpleIde.Models"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ZenPlatform.SimpleIde.Views.ConfigurationTreeView">
  <TreeView Grid.Column="0" Items="{Binding Configuration}" Name="TreeView">
    <TreeView.Styles>
      <Style Selector="TreeViewItem">
        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
       
      </Style>
    </TreeView.Styles>
    <TreeView.DataTemplates>
      <TreeDataTemplate DataType="models:IConfiguratoinItem" ItemsSource="{Binding Childs}"  >
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="18" IsEnabled="{Binding IsEnable}" >

          <Image Source="{Binding Image}"/>
          <TextBlock Text="{Binding Content}" />
        </StackPanel>
      </TreeDataTemplate>

    </TreeView.DataTemplates>
  </TreeView>
</UserControl>
